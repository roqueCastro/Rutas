<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/addConductor"
        android:title="Conductor"
        android:icon="@mipmap/ic_person_add_foreground"
        android:iconTint="@color/colorAccent"
        app:showAsAction="always"
        />
    <item
        android:id="@+id/addRutas"
        android:title="Rutas"
        android:icon="@mipmap/ic_add_ruta_foreground"
        android:iconTint="@color/colorAccent"
        app:showAsAction="always"
        />
    <item
        android:id="@+id/salir"
        android:title="Salir"
        android:icon="@mipmap/ic_salir_a_foreground"
        android:iconTint="@color/colorAccent"
        app:showAsAction="always"
        />
</menu>